# Missing Package - Install Instructions

The package `tailwindcss-animate` is required for the project but not included in package.json.

To install it:

```bash
cd frontend
npm install tailwindcss-animate
```

This package is used for Tailwind CSS animations in the UI components.
